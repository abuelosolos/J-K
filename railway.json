{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd fara-backend && npm install"
  },
  "deploy": {
    "startCommand": "cd fara-backend && node index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
